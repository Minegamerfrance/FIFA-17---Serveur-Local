import express from "express";
/**
 * Engagement stub — FIFA 17 connects to 127.0.0.1:42230.
 */
export declare function startEngagement(tls: {
    key: Buffer;
    cert: Buffer;
}): express.Express;
